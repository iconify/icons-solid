# @iconify-solid/whh

⚠️ This icon set is deprecated and no longer maintained. Use it at your own risk.


**WebHostingHub Glyphs**

Author: WebHostingHub

License: [Open Font License](https://scripts.sil.org/cms/scripts/page.php?site_id=nrsi&id=OFL)

Browse all icons: [preview WebHostingHub Glyphs on Iconify](https://icon-sets.iconify.design/whh/)

## Installation

```bash
npm install github:iconify/icons-solid#whh
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/whh/addtags';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/whh/brightness';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/whh/circlecallincoming';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
