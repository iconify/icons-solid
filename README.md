# @iconify-solid/dashicons

Icon set prefix: **dashicons**

Icon set name: **Dashicons**

Author: **WordPress**

License: **[GPL](https://github.com/WordPress/dashicons/blob/master/gpl.txt)**

Repository: **https://github.com/WordPress/dashicons**

Browse all icons: **[Preview Dashicons](https://icon-sets.iconify.design/dashicons/)**

## Installation

```bash
npm install github:iconify/icons-solid#dashicons
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/dashicons/shortcode';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/dashicons/businessperson';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/dashicons/editor-expand';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
