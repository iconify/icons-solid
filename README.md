# @iconify-solid/entypo

Icon set prefix: **entypo**

Icon set name: **Entypo+**

Author: **Daniel Bruce**

License: **[CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/)**

Repository: **https://github.com/chancancode/entypo-plus**

Browse all icons: **[Preview Entypo+](https://icon-sets.iconify.design/entypo/)**

## Installation

```bash
npm install github:iconify/icons-solid#entypo
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/entypo/bell';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/entypo/image';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/entypo/erase';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
