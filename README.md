# @iconify-solid/cuida

Icon set prefix: **cuida**

Icon set name: **Cuida Icons**

Author: **Sysvale**

License: **[Apache 2.0](https://github.com/Sysvale/cuida-icons/blob/main/LICENSE)**

Repository: **https://github.com/Sysvale/cuida-icons**

Browse all icons: **[Preview Cuida Icons](https://icon-sets.iconify.design/cuida/)**

## Installation

```bash
npm install github:iconify/icons-solid#cuida
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/cuida/sidebar-expand-outline';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/cuida/filter-outline';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/cuida/caret-up-outline';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
