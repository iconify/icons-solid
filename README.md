# @iconify-solid/boxicons

Icon set prefix: **boxicons**

Icon set name: **Boxicons**

Author: **Boxicons**

License: **[MIT](https://github.com/box-icons/boxicons-core/blob/main/LICENSE)**

Repository: **https://github.com/box-icons/boxicons-core**

Browse all icons: **[Preview Boxicons](https://icon-sets.iconify.design/boxicons/)**

## Installation

```bash
npm install github:iconify/icons-solid#boxicons
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/boxicons/clock-2-filled';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/boxicons/desk';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/boxicons/battery-filled';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
