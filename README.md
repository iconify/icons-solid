# @iconify-solid/wordpress

⚠️ This is an alternative repository. Main repository has moved to [GitLab](https://gitlab.com/iconify-icons/solid/-/tree/wordpress).
**WordPress Icons**

Author: [The WordPress Contributors](https://github.com/WordPress/gutenberg/tree/HEAD/packages/icons)

License: [GPL](https://github.com/WordPress/gutenberg/blob/HEAD/LICENSE.md#gnu-general-public-license-version-2)

Browse all icons: [preview WordPress Icons on Iconify](https://icon-sets.iconify.design/wordpress/)

## Installation

```bash
npm install gitlab:iconify-icons/solid#wordpress
```

or
```bash
npm install github:iconify/icons-solid#wordpress
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/wordpress/home';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/wordpress/pencil';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/wordpress/corner-top-right';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
