# @iconify-solid/garden

Icon set prefix: **garden**

Icon set name: **Garden SVG Icons**

Author: **Zendesk**

License: **[Apache 2.0](https://github.com/zendeskgarden/svg-icons/blob/main/LICENSE.md)**

Repository: **https://github.com/zendeskgarden/svg-icons**

Browse all icons: **[Preview Garden SVG Icons](https://icon-sets.iconify.design/garden/)**

## Installation

```bash
npm install github:iconify/icons-solid#garden
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/garden/asterisk-stroke-12';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/garden/bookmark-fill-12';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/garden/moon-fill-12';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
