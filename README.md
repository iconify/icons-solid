# @iconify-solid/vaadin

**Vaadin Icons**

Author: [Vaadin](https://github.com/vaadin/web-components)

License: Apache 2.0

Browse all icons: [preview Vaadin Icons on Iconify](https://icon-sets.iconify.design/vaadin/)

## Installation

```bash
npm install github:iconify/icons-solid#vaadin
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/vaadin/area-select';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/vaadin/file-picture';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/vaadin/plus-circle-o';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
