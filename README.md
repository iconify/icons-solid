# @iconify-solid/cib

Icon set prefix: **cib**

Icon set name: **CoreUI Brands**

Author: **creativeLabs Łukasz Holeczek**

License: **[CC0 1.0](https://creativecommons.org/publicdomain/zero/1.0/)**

Repository: **https://github.com/coreui/coreui-icons**

Browse all icons: **[Preview CoreUI Brands](https://icon-sets.iconify.design/cib/)**

## Installation

```bash
npm install github:iconify/icons-solid#cib
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/cib/cc-amazon-pay';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/cib/hotjar';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/cib/open-id';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
