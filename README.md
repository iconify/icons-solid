# @iconify-solid/bpmn

⚠️ This icon set is deprecated and no longer maintained. Use it at your own risk.


**BPMN**

Author: [Camunda Services GmbH](https://github.com/bpmn-io/bpmn-font)

License: [Open Font License](https://github.com/bpmn-io/bpmn-font/blob/master/LICENSE)

Browse all icons: [preview BPMN on Iconify](https://icon-sets.iconify.design/bpmn/)

## Installation

```bash
npm install github:iconify/icons-solid#bpmn
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/bpmn/intermediate-event-catch-non-interrupting-escalation';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/bpmn/user';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/bpmn/lane-insert-above';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
