# @iconify-solid/lets-icons

**Lets Icons**

Author: [Leonid Tsvetkov](https://www.figma.com/community/file/886554014393250663)

License: [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/)

Browse all icons: [preview Lets Icons on Iconify](https://icon-sets.iconify.design/lets-icons/)

## Installation

```bash
npm install github:iconify/icons-solid#lets-icons
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/lets-icons/search-duotone-line';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/lets-icons/view-alt';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/lets-icons/message-duotone';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
