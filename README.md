# @iconify-solid/pajamas

**Gitlab SVGs**

Author: [GitLab B.V.](https://gitlab.com/gitlab-org/gitlab-svgs/-/tree/main)

License: [MIT](https://gitlab.com/gitlab-org/gitlab-svgs/-/blob/main/LICENSE)

Browse all icons: [preview Gitlab SVGs on Iconify](https://icon-sets.iconify.design/pajamas/)

## Installation

```bash
npm install github:iconify/icons-solid#pajamas
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/pajamas/preferences';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/pajamas/expire';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/pajamas/merge';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
