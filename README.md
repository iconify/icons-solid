# @iconify-solid/circum

Icon set prefix: **circum**

Icon set name: **Circum Icons**

Author: **Klarr Agency**

License: **[Mozilla Public License 2.0](https://github.com/Klarr-Agency/Circum-Icons/blob/main/LICENSE)**

Repository: **https://github.com/Klarr-Agency/Circum-Icons**

Browse all icons: **[Preview Circum Icons](https://icon-sets.iconify.design/circum/)**

## Installation

```bash
npm install github:iconify/icons-solid#circum
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/circum/text';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/circum/pill';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/circum/zoom-out';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
