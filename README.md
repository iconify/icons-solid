# @iconify-solid/il

⚠️ This icon set is deprecated and no longer maintained. Use it at your own risk.


**Icalicons**

Author: Icalia Labs

License: MIT

Browse all icons: [preview Icalicons on Iconify](https://icon-sets.iconify.design/il/)

## Installation

```bash
npm install github:iconify/icons-solid#il
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/il/calendar';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/il/users';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/il/conversation';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
