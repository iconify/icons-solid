# @iconify-solid/tabler

⚠️ This is an alternative repository. Main repository has moved to [GitLab](https://gitlab.com/iconify-icons/solid/-/tree/tabler).
**Tabler Icons**

Author: [Paweł Kuna](https://github.com/tabler/tabler-icons)

License: [MIT](https://github.com/tabler/tabler-icons/blob/master/LICENSE)

Browse all icons: [preview Tabler Icons on Iconify](https://icon-sets.iconify.design/tabler/)

## Installation

```bash
npm install gitlab:iconify-icons/solid#tabler
```

or
```bash
npm install github:iconify/icons-solid#tabler
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/tabler/alien';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/tabler/device-desktop';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/tabler/photo';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
