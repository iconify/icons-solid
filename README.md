# @iconify-solid/si-glyph

⚠️ This icon set is deprecated and no longer maintained. Use it at your own risk.


**SmartIcons Glyph**

Author: SmartIcons

License: [CC BY SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/)

Browse all icons: [preview SmartIcons Glyph on Iconify](https://icon-sets.iconify.design/si-glyph/)

## Installation

```bash
npm install github:iconify/icons-solid#si-glyph
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/si-glyph/circle-load-left';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/si-glyph/basket-arrow-right';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/si-glyph/slide-show';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
