# @iconify-solid/nonicons

**Nonicons**

Author: [yamatsum](https://github.com/yamatsum/nonicons)

License: [MIT](https://github.com/yamatsum/nonicons/blob/master/LICENSE)

Browse all icons: [preview Nonicons on Iconify](https://icon-sets.iconify.design/nonicons/)

## Installation

```bash
npm install github:iconify/icons-solid#nonicons
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/nonicons/kotlin-16';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/nonicons/vue-16';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/nonicons/npm-16';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
