# @iconify-solid/bi

Icon set prefix: **bi**

Icon set name: **Bootstrap Icons**

Author: **The Bootstrap Authors**

License: **[MIT](https://github.com/twbs/icons/blob/main/LICENSE.md)**

Repository: **https://github.com/twbs/icons**

Browse all icons: **[Preview Bootstrap Icons](https://icon-sets.iconify.design/bi/)**

## Installation

```bash
npm install github:iconify/icons-solid#bi
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/bi/graph-up';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/bi/card-image';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/bi/code-slash';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
