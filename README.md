# @iconify-solid/fa

Icon set prefix: **fa**

Icon set name: **Font Awesome 4**

Author: **Dave Gandy**

License: **[Open Font License](https://scripts.sil.org/cms/scripts/page.php?site_id=nrsi&id=OFL)**

Repository: **https://github.com/FortAwesome/Font-Awesome/tree/fa-4**

Browse all icons: **[Preview Font Awesome 4](https://icon-sets.iconify.design/fa/)**

## Installation

```bash
npm install github:iconify/icons-solid#fa
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/fa/wrench';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/fa/bell-o';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/fa/user-o';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
