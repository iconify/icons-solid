# @iconify-solid/uiw

**uiw icons**

Author: [liwen0526](https://github.com/uiwjs/icons)

License: [MIT](https://github.com/uiwjs/icons/blob/master/LICENSE)

Browse all icons: [preview uiw icons on Iconify](https://icon-sets.iconify.design/uiw/)

## Installation

```bash
npm install github:iconify/icons-solid#uiw
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/uiw/cut';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/uiw/like-o';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/uiw/download';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
