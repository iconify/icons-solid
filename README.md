# @iconify-solid/famicons

Icon set prefix: **famicons**

Icon set name: **Famicons**

Author: **Family**

License: **[MIT](https://github.com/familyjs/famicons/blob/main/LICENSE)**

Repository: **https://github.com/familyjs/famicons**

Browse all icons: **[Preview Famicons](https://icon-sets.iconify.design/famicons/)**

## Installation

```bash
npm install github:iconify/icons-solid#famicons
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/famicons/backspace-outline';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/famicons/home-sharp';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/famicons/bluetooth-outline';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
