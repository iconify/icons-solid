# @iconify-solid/bx

Icon set prefix: **bx**

Icon set name: **BoxIcons v2**

Author: **Boxicons**

License: **[MIT](https://github.com/box-icons/boxicons/blob/main/LICENSE)**

Repository: **https://github.com/box-icons/boxicons**

Browse all icons: **[Preview BoxIcons v2](https://icon-sets.iconify.design/bx/)**

## Installation

```bash
npm install github:iconify/icons-solid#bx
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/bx/heart-circle';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/bx/last-page';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/bx/bar-chart-square';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
