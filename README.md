# @iconify-solid/el

Icon set prefix: **el**

Icon set name: **Elusive Icons**

Author: **Team Redux**

License: **[Open Font License](https://scripts.sil.org/cms/scripts/page.php?site_id=nrsi&id=OFL)**

Repository: **https://github.com/dovy/elusive-icons**

Browse all icons: **[Preview Elusive Icons](https://icon-sets.iconify.design/el/)**

## Installation

```bash
npm install github:iconify/icons-solid#el
```

## Usage

For full documentation visit [Iconify website](https://iconify.design/docs/usage/svg-css/solid/).

Few usage examples:

```jsx
import SampleIcon from '@iconify-solid/el/headphones';

function Test() {
  return (
    <SampleIcon />
  );
}
```

To resize icon, set `width` and/or `height` parameters:

```jsx
import SampleIcon from '@iconify-solid/el/cog';

function Test() {
  return (
    <SampleIcon height="1em" />
  );
}
```

To change icon color, use style:

```jsx
import SampleIcon from '@iconify-solid/el/user';

function Test() {
  return (
    <SampleIcon style={{color: 'red'}} />
  );
}
```
